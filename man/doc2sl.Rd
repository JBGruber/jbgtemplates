% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2semantic-linebreaks.r
\name{doc2sl}
\alias{doc2sl}
\title{Convert a document to semantic linebreaks}
\usage{
doc2sl(input, output = input)
}
\arguments{
\item{input}{Path to the input file (character scalar).}

\item{output}{Path to the output file. Defaults to `input`, i.e. the file is
edited in place.}
}
\description{
Rewrites a plain-text, Markdown, or LaTeX file so that each sentence
occupies exactly one source line — a convention known as *semantic
linebreaks* (also *semantic linefeeds* or *ventilated prose*).
}
\details{
**What are semantic linebreaks?**
The [sembr.org](https://sembr.org/) specification defines semantic
linebreaks as inserting line breaks at *semantic boundaries* — typically
sentence or clause boundaries — rather than at a fixed column width.
The source file becomes easier for humans to navigate and edit; the
rendered output is unaffected.

**Why does this work in Markdown and LaTeX?**
Both formats treat a single newline inside a paragraph as whitespace, so
splitting one long paragraph line into many one-sentence lines produces
identical rendered output.
A blank line is still required to start a new paragraph.

**Why does it matter for git?**
With sentences on separate lines, `git diff` shows exactly which sentences
changed.
Conventional hard-wrapped text (e.g. at 80 characters) causes a single
word edit to reflow the entire paragraph, drowning the real change in noise.
Semantic linebreaks make pull-request reviews, co-author edits, and
`git blame` all dramatically more readable.


Sentence tokenisation is performed by
[tokenizers::tokenize_sentences()], which handles inline LaTeX commands
gracefully.
}
\examples{
\dontrun{
doc2sl("main.tex")               # edit in place
doc2sl("draft.md", "out.md")     # write to a new file
}
}
\references{
Gruber, J. (2004). Markdown syntax.
<https://daringfireball.net/projects/markdown/>

Semantic Line Breaks specification. <https://sembr.org/>

Stacey, S. (2025). Semantic line breaks are a feature of Markdown, not a
bug. *Writing Slowly*.
<https://writingslowly.com/2025/01/13/semantic-line-breaks-are-a.html>
}
\seealso{
[tokenizers::tokenize_sentences()]
}
